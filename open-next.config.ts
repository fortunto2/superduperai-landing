import { defineCloudflareConfig } from "@opennextjs/cloudflare";

// Используем базовую конфигурацию, статичность управляется через Next.js config и экспорты страниц
export default defineCloudflareConfig(); 